@page
@model SalesTracker.Pages.AwardsNotices.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AwardNotice[0].isNotice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AwardNotice[0].EntryDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AwardNotice[0].SalesPerson)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AwardNotice) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.isNotice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SalesPerson.FirstName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
